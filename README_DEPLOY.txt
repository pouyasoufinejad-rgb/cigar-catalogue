CIGAR CATALOGUE v136 — CLOUDFLARE WORKERS DEPLOYMENT

Built directly from the exact live Cloudflare HTML snapshot captured after rollback.
Preserves the saved card edits, archived cards, images and working laurel system.

Adds:
- real 1–10 sorting for Strength / Quality / Value
- All recommendations includes unavailable cards
- editable Production / Practical markup
- editable Legend & scoring guide
- Benchmarks rename + editable markup
- Cloudflare KV persistence for cards and global sections
- embedded live snapshot fallback if KV is empty

DEPLOY
Extract and double-click DEPLOY.bat. It targets the existing Worker named cigar-catalogue.
