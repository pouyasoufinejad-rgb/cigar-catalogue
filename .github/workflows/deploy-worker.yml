name: Verify catalogue Worker deployment

on:
  pull_request:
    paths:
      - '.github/workflows/deploy-worker.yml'
      - 'public/**'
      - 'src/**'
      - 'scripts/verify-live-code-ready.mjs'
      - 'scripts/verify-live-recommendation-v4.mjs'
      - 'scripts/build-recommendation-subsections-migration.mjs'
      - 'test/**'
      - 'wrangler.jsonc'
      - 'package.json'
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-worker.yml'
      - 'public/**'
      - 'src/**'
      - 'scripts/verify-live-code-ready.mjs'
      - 'scripts/verify-live-recommendation-v4.mjs'
      - 'scripts/build-recommendation-subsections-migration.mjs'
      - 'test/**'
      - 'wrangler.jsonc'
      - 'package.json'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cigar-catalogue-worker-deploy
  cancel-in-progress: false

jobs:
  verify:
    name: Test Worker deploy candidate
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run tests
        shell: bash
        run: |
          set -o pipefail
          npm test 2>&1 | tee npm-test.log
      - name: Upload test log
        if: always() && github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: npm-test-log
          path: npm-test.log
          retention-days: 1
          if-no-files-found: error

  verify-live:
    name: Verify Cloudflare Workers Build
    needs: verify
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Wait for v6 runtime and compatible v4 code to be live
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 20); do
            if node scripts/verify-live-code-ready.mjs; then
              exit 0
            fi
            sleep 6
          done
          echo "::error::Cloudflare Workers Build did not expose the v6 runtime and compatible v4 code."
          exit 1
      - name: Build fresh read-only migration candidate from deployed production
        shell: bash
        run: |
          set -euo pipefail
          node scripts/build-recommendation-subsections-migration.mjs | tee recommendation-subsections-migration-candidate.json
      - name: Upload post-deploy migration candidate
        uses: actions/upload-artifact@v4
        with:
          name: recommendation-subsections-migration-candidate-post-deploy
          path: recommendation-subsections-migration-candidate.json
          retention-days: 1
          if-no-files-found: error
