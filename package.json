{
  "name": "cigar-catalogue-workers",
  "private": true,
  "version": "136.0.0",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "publish:catalogue": "node scripts/publish-live-catalogue-request.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "wrangler": "^4.0.0"
  }
}
