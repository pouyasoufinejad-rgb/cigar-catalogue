{
  "name": "cigar-catalogue-workers",
  "private": true,
  "version": "136.0.0",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "publish:catalogue": "node scripts/publish-catalogue-request.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
